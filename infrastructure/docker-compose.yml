version: '2'
services:
  nginx:
    build: .
    container_name: "wee-nginx"
    ports:
      - "10080:80"
    networks:
      - wee-net
    volumes:
      - ../config.conf:/etc/nginx/nginx.conf

networks:
  wee-net:
    driver: bridge
